# Session — 2026-09-03

Sitzung lief vom 2026-09-01 bis 2026-09-03 durch (Statuszeile, Icons, Leisten, Resume-Bug, Regeln).

## Decisions

- Hauptzeile der Statuszeile zentriert, sobald sie einzeilig passt und das Panel > 400px ist;
  Messung statt Breakpoint (`updateCentering` + `ResizeObserver`). Editor- und cwd-Zeile links.
- Abstände der Statuszeile bleiben 12/24 — die 8px der Figma-Mocks sind schematisch.
- Icons: `semibold`, 44pt mit Deckel pro Symbol (resume 40, continue/restart 42); `plus.png` 36pt;
  `+`-Glyphen in beiden Leisten 11px.
- Gruppenleiste über der Tab-Leiste; Linie oben und unten, keine Seitenlinien, keine eigenen
  Radien (VS Code rahmt und rundet die Sekundärleiste selbst); `+` ohne eigenes `border-left`.
- Resume-Abbruch: Rettungsplan pro Tab — `--continue`, dann frisch, dann Exit-Zeile; neuer Tab nur
  frisch. `Restart`/`Continue` und jeder andere Exit-Code verwerfen den Plan.
- Shift+Pfeil-Auswahl im Prompt ist Claude-Code-Sache, nicht Panel; Maus-Markierung reicht.
- Regeln in `.claude/rules/*.md` (pfadgebunden), Geschichten in `LEARNINGS.md`; `.gitignore`
  versteckt nur noch `settings.local.json`.

## Open items

- `094de94` und die Pre-Clear-Änderungen sind nicht gepusht bzw. nicht committet.
- `Sessions/**` fehlt in `.vscodeignore`; die Handoff-Dateien fahren im `.vsix` mit.
- Reload für `e71d73b` steht aus — Resume → ESC im echten Panel noch nicht gesehen.
- `--continue` holt die jüngste Sitzung des Verzeichnisses; ein zweiter Tab im selben Ordner kann
  dazwischenfunken. Keine Session-ID im Panel.

## Next concrete steps

- Commit + Push der Doku, `Sessions/**` in `.vscodeignore`, `vsce ls | grep Sessions` leer.
- Reload, `Resume` → ESC prüfen, dann `New Terminal Tab (Resume Session…)` → ESC.
